# SPR-0004B.6 Rev.6 — Integration

1. Open the local repository root:

```text
.../microlino-open-telemetry/
```

2. Extract `SPR-0004B.6-REV6-RETURN-MECHANISM-PACKAGE.zip` directly into that directory.

3. Overwrite the existing files.

4. Verify that the resulting paths are:

```text
firmware/esp32-wroom/src/web/web_ui.cpp
firmware/lilygo-t-a7670/src/web/lilygo_web.cpp
```

5. Compile and test before committing to `develop`.

No `git apply`, wrapper-directory removal or path adjustment is required.
