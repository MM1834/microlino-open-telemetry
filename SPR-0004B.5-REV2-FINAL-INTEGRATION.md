# SPR-0004B.5 Rev.2 Finalization

Apply this patch after SPR-0004B.5 Rev.2 has already been integrated.

```bash
patch --dry-run -p1 < SPR-0004B.5-REV2-FINAL.patch
patch -p1 < SPR-0004B.5-REV2-FINAL.patch
```

Build variants:

```bash
cd firmware/esp32-wroom
pio run -e esp32dev
pio run -e esp32dev-aws

cd ../lilygo-t-a7670
pio run -e lilygo-t-a7670
pio run -e T-A7670X-AWS
```

Expected visible versions:

- `SPR-0004B.5R2-MQTT`
- `SPR-0004B.5R2-AWS`

The build variant remains compile-time information until AWS and private MQTT transports are unified.
