# CAN Decoder

The v1.0.0 firmware focuses on Microlino display CAN data. The decoder writes normalized values into the telemetry model.

Future decoders may add BMS, cell voltage and temperature data.
