# Firmware Configuration

The firmware stores configuration in non-volatile preferences.

Configurable:
- WiFi SSID/password
- MQTT host, port and credentials
- MQTT prefix
- Vehicle ID
- Vehicle name
- OTA password
