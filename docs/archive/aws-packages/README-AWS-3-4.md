# AWS-3.4 Dashboard REST

Expected header detail:

```text
Verbunden mit AWS Vehicle API
```

The configured API URL is:

```text
https://yaugi9zu8l.execute-api.eu-north-1.amazonaws.com
```

Perform a hard browser reload after copying the files.
