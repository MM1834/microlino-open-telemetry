# AWS-3.4 Dashboard cleanup

Expected header:

```text
Cloud
Online
Verbunden mit AWS Vehicle API
```

The legacy MQTT implementation remains available in the repository as an
optional debug path, but is not loaded by the production page.
