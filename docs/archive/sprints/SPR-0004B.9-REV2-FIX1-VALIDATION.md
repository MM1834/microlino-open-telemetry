# SPR-0004B.9-REV2-FIX1 Validation

1. Overlay the package at repository root.
2. Clean and rebuild the ESP32 AWS environment.
3. Flash the ESP32.
4. Confirm boot log and WebUI show `SPR-0004B.9-REV2-AWS`.
5. Build the MQTT environment and confirm `SPR-0004B.9-REV2-MQTT` in the binary/runtime output.
6. Build and flash LilyGO; confirm the corresponding REV2 version.

A clean build is recommended because PlatformIO may otherwise reuse compiled objects containing the old version string.
