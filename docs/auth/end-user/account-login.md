# Account login

The authentication foundation is being introduced in stages.

The system now contains:

- a protected Cognito user directory;
- a dashboard application registration.

A visible login page is not available in this increment. The next steps add the Cognito login domain and later connect the dashboard. Existing telemetry and dashboard behavior remain unchanged until the login integration is activated.
