# Account login

The project now has the Cognito login endpoint required for browser authentication. The dashboard login screen and the complete redirect flow are implemented in a later increment.

When login is enabled, users will sign in with their verified email address and password. Public self-registration remains disabled; an administrator creates or invites accounts.
