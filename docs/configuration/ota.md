# OTA Configuration

OTA is available from the local Web UI.

```text
/ota
```

If an OTA password is configured, it must be entered before uploading firmware.

If no OTA password is configured, OTA is allowed without a password. This is convenient during development but should be considered before production use.
